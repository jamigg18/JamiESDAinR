tm_scalebar(position = c("center", "bottom")) +
tm_compass(type = "8star", position = c("right", "bottom"))
map_local_elev
sa_waters <- sa_water %>% mutate(intersects_park = length(sf::st_intersects(sa_parks, sa_water)) > 0)
sa_water$intersects_park
glimpse(sa_waters)
glimpse(sa_water$intersects_park)
glimpse(sa_waters$intersects_park)
sa_waters$intersects_park
glimpse(sa_waters$intersects_park)
sa_waters <- sa_water %>% mutate(intersects_park = length(sf::st_intersects(., sa_parks)) > 0)
glimpse(sa_waters$intersects_park)
glimpse(sa_waters$intersects_park)
sa_waters$intersects_park
glimpse(sa_waters$intersects_park)
port_water <- sf::read_sf("./data/static_mapping/tl_2022_39133_linearwater/") %>% st_make_valid
summ_water <- sf::read_sf("./data/static_mapping/tl_2023_39153_linearwater/") %>% st_make_valid
sa_water <- dplyr::bind_rows(port_water, summ_water) %>% st_transform(st_crs(sa))
sa_waters <- sa_water %>% mutate(intersects_park = lengths(sf::st_intersects(., sa_parks)) > 0)
glimpse(sa_waters$intersects_park)
glimpse(sa_waters$intersects_park)
#streams <- tm_shape(sa_water) + tm_lines(lwd = 1.5, col = "ARTPATH", palette="PuBu")
streams <- tm_shape(sa_waters) + tm_lines(led = 1.5, col = "intersects_park", palette="PuBu")
streams
streams <- tm_shape(sa_waters) + tm_lines(led = 1.5, col = "intersects_park", palette="PuBu")
streams
map_local <- park_feats + streams + municipal_boundaries
map_local
streams <- tm_shape(sa_waters) + tm_lines(led = 1.5, col = "intersects_park", palette="Blues")
streams
streams <- tm_shape(sa_waters) + tm_lines(led = 1.5, col = "intersects_park", palette="PuBu")
streams
streams <- tm_shape(sa_waters) + tm_lines(led = 1.5, col = "intersects_park", palette="Paired")
streams
map_local <- park_feats + streams + municipal_boundaries
map_local
streams <- tm_shape(sa_waters) + tm_lines(lwd = 1.5, col = "intersects_park", palette="Paired")
streams
map_local <- park_feats + streams + municipal_boundaries
map_local
dem <- terra::rast("./data/static_mapping/neoh_dem.tif") %>% terra::project(., sf::st_crs(sa)$wkt) %>% terra::crop(., sa)
#I chose to use the pipe hear to do this is one line of code, and I used the example of this from that inclass_static_mapping.Rmd, but I chose to project it first because I tried it and it did not take forever
dem_layer <- tm_shape(dem) +
tm_raster(col.scale = tm_scale_continuous(values = "terrain")) +
tm_shape(sa) + tm_polygons(fill_alpha = 0.3)
dem_layer
map_local_elev <- dem_layer + park_feats + streams +
tm_scalebar(position = c("center", "bottom")) +
tm_compass(type = "8star", position = c("right", "bottom"))
map_local_elev
dem_layer
map_local_elev <- dem_layer + park_feats + streams +
tm_scalebar(position = c("center", "bottom")) +
tm_compass(type = "8star", position = c("right", "bottom"))
map_local_elev
tmap_arrange(map_ohio, map_local_elev)
tmap_arrange(map_ohio, map_local_elev) + tm_layout(asp = 0)
tmap_arrange(map_ohio, map_local_elev) + tm_layout(asp = NA)
tmap_arrange(map_ohio, map_local_elev, asp = NA)
tmap_arrange(map_ohio, map_local_elev, asp = 0)
tmap_arrange(map_ohio, map_local_elev, ncol = 2)
tmap_arrange(map_ohio, map_local_elev, ncol = 2, asp = 0)
tmap_arrange(map_ohio, map_local_elev, ncol = 1, asp = 0)
tmap_arrange(map_ohio, map_local_elev, ncol = 1, asp = NA)
map_inset <- tm_shape(ohio_counties) + tm_polygons() +
tm_shape(oh_region) + tm_borders(lwd = 2, col = "red") +
tm_layout(bg.color = "lightblue") +
tm_scalebar(position = c("left", "bottom"))
map_inset
map_inset <- tm_shape(ohio_counties) + tm_polygons() +
tm_shape(oh_region) + tm_borders(lwd = 2.5, col = "red") +
tm_layout(bg.color = "lightblue") +
tm_scalebar(position = c("left", "bottom"))
map_inset
map_inset <- tm_shape(ohio_counties) + tm_polygons() +
tm_shape(oh_region) + tm_borders(lwd = 2.5, col = "red") +
tm_layout(bg.color = "lightblue") +
tm_scalebar(position = c("left", "bottom"))
map_inset
map_inset <- tm_shape(ohio_counties) + tm_polygons() +
tm_shape(oh_region) + tm_borders(lwd = 3, col = "red") +
tm_layout(bg.color = "lightblue") +
tm_scalebar(position = c("left", "bottom"))
map_inset
return(unit(c(w, h), "snpc"))
norm_dim <- function(obj) {
bbox <- st_bbox(obj)
width  <- bbox[["xmax"]] - bbox[["xmin"]]
height <- bbox[["ymax"]] - bbox[["ymin"]]
w <- width  / max(width, height)
h <- height / max(width, height)
return(unit(c(w, h), "snpc"))
}
# Calculate dimensions for both maps
# The main map is based on the study area bounding box (oh_region)
# The inset map is based on all of Ohio
main_dim <- norm_dim(oh_region)  # fill in: use oh_region
ins_dim  <- norm_dim(ohio_counties)  # fill in: use the all-Ohio sf object
main_vp <- viewport(width = main_dim[1], height = main_dim[2])
ins_vp <- viewport(
width  = ins_dim[1] * 0.4,              # scale the inset to 40% of normalized Ohio width
height = ins_dim[2] * 0.4,              # scale proportionally
x      = unit(1, "npc") - unit(0.5, "cm"),  # right edge of device minus a small margin
y      = unit(0.01, "npc"),             # near the bottom of the device
just   = c("right", "bottom")           # anchor point for the inset
)
grid.newpage()
print(map_local_elev, vp = main_vp)
pushViewport(main_vp)
print(map_inset, vp = ins_vp)
grid.newpage()
print(map_local_elev, vp = main_vp)
pushViewport(main_vp)
print(map_inset, vp = ins_vp)
# ---- Step 1: Build the inset (context) map ----
# This is the small map of all of Ohio that shows where our study area is.
# oh_region is the bounding box polygon you created in Task 2.1.
map_inset <- tm_shape(ohio_counties) + tm_polygons() +
tm_shape(oh_region) + tm_borders(lwd = 3, col = "red") +
tm_layout(bg.color = "lightblue") +
tm_scalebar(position = c("left", "bottom"))
# ---- Step 2: Normalize dimensions ----
# A viewport is defined in "normalized parent coordinates" (npc) from 0 to 1.
# If we just use width and height directly, maps with very different aspect ratios will appear distorted. norm_dim() calculates the width and height as fractions of the longest dimension, so that the aspect ratio is preserved.
norm_dim <- function(obj) {
bbox <- st_bbox(obj)
width  <- bbox[["xmax"]] - bbox[["xmin"]]
height <- bbox[["ymax"]] - bbox[["ymin"]]
w <- width  / max(width, height)
h <- height / max(width, height)
return(unit(c(w, h), "snpc"))
}
# Calculate dimensions for both maps
# The main map is based on the study area bounding box (oh_region)
# The inset map is based on all of Ohio
main_dim <- norm_dim(oh_region)  # fill in: use oh_region
ins_dim  <- norm_dim(ohio_counties)  # fill in: use the all-Ohio sf object
# ---- Step 3: Define viewport positions ----
# A viewport is a rectangular region of a graphics device.
# main_vp defines the size of the main map within the graphics window.
# ins_vp defines the size AND position of the inset.
main_vp <- viewport(width = main_dim[1], height = main_dim[2])
ins_vp <- viewport(
width  = ins_dim[1] * 0.4,              # scale the inset to 40% of normalized Ohio width
height = ins_dim[2] * 0.4,              # scale proportionally
x      = unit(1, "npc") - unit(0.5, "cm"),  # right edge of device minus a small margin
y      = unit(0.01, "npc"),             # near the bottom of the device
just   = c("right", "bottom")           # anchor point for the inset
)
# ---- Step 4: Draw ----
# grid.newpage() clears the device.
# print(..., vp = ...) draws a tmap within a specific viewport.
# pushViewport() makes the main viewport the "active" coordinate system, so the inset is positioned relative to the main map, not the whole device.
grid.newpage()
print(map_local_elev, vp = main_vp)
pushViewport(main_vp)
print(map_inset, vp = ins_vp)
grid.newpage()
print(map_inset, vp = ins_vp)
pushViewport(main_vp)
print(map_local_elev, vp = main_vp)
grid.newpage()
print(map_local_elev, vp = main_vp)
pushViewport(main_vp)
print(map_inset, vp = ins_vp)
ins_vp <- viewport(
width  = ins_dim[1] * 0.2,              # scale the inset to 40% of normalized Ohio width
height = ins_dim[2] * 0.2,              # scale proportionally
x      = unit(1, "npc") - unit(0.2, "cm"),  # right edge of device minus a small margin
y      = unit(0.01, "npc"),             # near the bottom of the device
just   = c("right", "bottom")           # anchor point for the inset
)
grid.newpage()
print(map_local_elev, vp = main_vp)
pushViewport(main_vp)
print(map_inset, vp = ins_vp)
grid.newpage()
print(map_inset, vp = ins_vp)
print(map_inset, vp = ins_vp)
ins_vp <- viewport(
width  = ins_dim[1] * 0.4,              # scale the inset to 40% of normalized Ohio width
height = ins_dim[2] * 0.4,              # scale proportionally
x      = unit(1, "npc") - unit(0.5, "cm"),  # right edge of device minus a small margin
y      = unit(0.01, "npc"),             # near the bottom of the device
just   = c("right", "bottom")           # anchor point for the inset
)
print(map_inset, vp = ins_vp)
grid.newpage()
print(map_inset, vp = ins_vp)
print(map_local_elev, vp = main_vp)
pushViewport(main_vp)
grid.newpage()
print(map_local_elev, vp = main_vp)
pushViewport(main_vp)
print(map_inset, vp = ins_vp)
print(map_inset, vp = ins_vp, component.autoscale = FALSE)
grid.newpage()
print(map_local_elev, vp = main_vp)
pushViewport(main_vp)
print(map_inset, vp = ins_vp)
map_inset
ins_vp <- viewport(
width  = ins_dim[1] * 0.3,              # scale the inset to 40% of normalized Ohio width
height = ins_dim[2] * 0.3,              # scale proportionally
x      = unit(1, "npc") - unit(0.5, "cm"),  # right edge of device minus a small margin
y      = unit(0.01, "npc"),             # near the bottom of the device
just   = c("right", "bottom")           # anchor point for the inset
)
grid.newpage()
print(map_local_elev, vp = main_vp)
pushViewport(main_vp)
print(map_inset, vp = ins_vp)
print(map_local_elev, vp = main_vp)
grid.newpage()
print(map_inset, vp = ins_vp)
grid.newpage()
print(map_inset, vp = ins_vp)
ins_vp <- viewport(
width  = ins_dim[1] * 0.4,          # scale the inset to 40% of normalized Ohio width
height = ins_dim[2] * 0.4,          # scale proportionally
x      = unit(1, "npc") - unit(0.5, "cm"),  # right edge of device minus a small margin
y      = unit(0.01, "npc"),          # near the bottom of the device
just   = c("right", "bottom")        # anchor point for the inset
)
print(map_inset, vp = ins_vp)
grid.newpage()
print(map_inset, vp = ins_vp)
print(map_local_elev, vp = main_vp)
grid.newpage()
print(map_local_elev, vp = main_vp)
pushViewport(main_vp)
print(map_inset, vp = ins_vp, component.autoscale = FALSE)
grid.newpage()
print(map_local_elev, vp = main_vp, tm_title("Map"))
grid.newpage()
print(map_local_elev, vp = main_vp, tm_title("Map"))
grid.newpage()
print(map_local_elev, vp = main_vp, tm_layout(title = "Map"))
grid.newpage()
print(map_local_elev, vp = main_vp, tm_title("Map"))
grid.newpage()
print(map_local_elev, vp = main_vp)
tm_title("Map")
grid.newpage()
print(map_local_elev(tm_title("Map")), vp = main_vp)
print(map_local_elev, (tm_title("Map")), vp = main_vp)
grid.newpage()
print(map_local_elev, tm_title("Map"), vp = main_vp)
print(map_local_elev, tm_title(Map), vp = main_vp)
county_join
glimpse(county_join)
map_ohio <- tm_shape(county_join) + tm_polygons(fill = "poptotal", fill.scale = tm_scale_intervals(style = "jenks", values = "brewer.purples", n = 5), fill.legend = tm_legend(title = "Total Population")) + tm_borders(lwd = 2, col = "black") + tm_scalebar(position = c("right", "bottom"))
map_ohio
map_ohio <- tm_shape(county_join) + tm_polygons(fill = "poptotal", fill.scale = tm_scale_intervals(style = "equal", values = "brewer.purples", n = 5), fill.legend = tm_legend(title = "Total Population")) + tm_borders(lwd = 2, col = "black") + tm_scalebar(position = c("right", "bottom"))
map_ohio
map_ohio <- tm_shape(county_join) + tm_polygons(fill = "poptotal", fill.scale = tm_scale_intervals(style = "jenks", values = "brewer.purples", n = 5), fill.legend = tm_legend(title = "Total Population")) + tm_borders(lwd = 2, col = "black") + tm_scalebar(position = c("right", "bottom"))
map_ohio <- tm_shape(county_join) + tm_polygons(fill = "poptotal", fill.scale = tm_scale_intervals(style = "jenks", values = "brewer.purples", n = 5), fill.legend = tm_legend(title = "Total Population")) + tm_borders(lwd = 2, col = "black") + tm_scalebar(position = c("right", "bottom"))
map_ohio
glimpse(county_join)
county_pop <- county_join %>% mutate(pop_per_area = as.numeric(poptotal) / (ALAND))
glimpse(county_pop)
map_ohio <- tm_shape(county_pop) + tm_polygons(fill = "pop_per_area", fill.scale = tm_scale_intervals(style = "jenks", values = "brewer.purples", n = 5), fill.legend = tm_legend(title = "Total Population per unit of Land Area")) + tm_borders(lwd = 2, col = "black") + tm_scalebar(position = c("right", "bottom"))
map_ohio
#read in raster data to look at land cover, project it to be the same crs as the ohio data, and crop it to the ohio data
land_cover <- terra::rast("./data/ohio/neoh_nlcd.tif") %>% terra::project(., sf::st_crs(county_pop)$wkt) %>% terra::crop(., county_pop)
lc_map <- tm_shape(land_cover) +
tm_raster(col.scale = tm_scale_continuous(values = "terrain")) +
tm_shape(sa) + tm_polygons(fill_alpha = 0.3)
lc_map
lc_map <- tm_shape(land_cover) +
tm_raster(col.scale = tm_scale_continuous(values = "terrain")) +
tm_shape(ohio_counties) + tm_borders()
lc_map
sf::st_crs(ohio_counties) == sf::st_crs(county_pop)
sf::st_crs(ohio_counties) == sf::st_crs(land_cover)
northeast_oh <- county_pop %>% dplyr::filter(NAME %in% c("Portage", "Summit", "Ashtabula", "Lake", "Geauga", "Trumbull", "Cuyahoga", "Mahoning", "Stark", "Lorain", "Medina"))
#made choropleth map to show ohio county populations per land area
pop_map <- tm_shape(northeast_oh) + tm_polygons(fill = "pop_per_area", fill.scale = tm_scale_intervals(style = "jenks", values = "brewer.purples", n = 5), fill.legend = tm_legend(title = "Total Population per unit of Land Area")) + tm_borders(lwd = 2, col = "black") + tm_scalebar(position = c("right", "bottom"))
pop_map
northeast_oh <- county_pop %>% dplyr::filter(NAME %in% c("Portage", "Summit", "Ashtabula", "Lake", "Geauga", "Trumbull", "Cuyahoga", "Mahoning", "Stark", "Lorain", "Medina", "Wayne", "Columbiana"))
#made choropleth map to show ohio county populations per land area
pop_map <- tm_shape(northeast_oh) + tm_polygons(fill = "pop_per_area", fill.scale = tm_scale_intervals(style = "jenks", values = "brewer.purples", n = 5), fill.legend = tm_legend(title = "Total Population per unit of Land Area")) + tm_borders(lwd = 2, col = "black") + tm_scalebar(position = c("right", "bottom"))
pop_map
#read in raster data to look at land cover, project it to be the same crs as the ohio data, and crop it to the northeastern ohio data
land_cover <- terra::rast("./data/ohio/neoh_nlcd.tif") %>% terra::project(., sf::st_crs(county_pop)$wkt) %>% terra::crop(., northeast_oh)
lc_map <- tm_shape(land_cover) +
tm_raster(col.scale = tm_scale_continuous(values = "terrain")) +
tm_shape(northeast_oh) + tm_borders()
lc_map
lc_map <- tm_shape(land_cover) +
tm_raster(col.scale = tm_scale_continuous(values = "terrain")) +
tm_shape(northeast_oh) + tm_borders(lwd = 2)
lc_map
#map the raster land cover data for northeastern ohio
lc_map <- tm_shape(land_cover) +
tm_raster(col.scale = tm_scale_continuous(values = "terrain")) +
tm_shape(northeast_oh) + tm_borders(lwd = 2, col = "black")
lc_map
tmap_arrange(pop_map, lc_map)
tmap_arrange(pop_map, lc_map, ncol = 1, asp = NA)
map_ohio
map_ohio <- tm_shape(county_census) + tm_polygons(fill = "females20to24", fill.scale = tm_scale_intervals(style = "jenks", values = "brewer.purples", n = 5), fill.legend = tm_legend(title = "Proportion of Females that are Ages 20-24")) + tm_borders(lwd = 2, col = "black") + tm_scalebar(position = c("right", "bottom"))
map_ohio
pop_map
#arrange the maps side-by-side to view pattern
tmap_arrange(pop_map, lc_map, ncol = 1, asp = NA)
library(sf)
library(terra)
library(tmap)
library(tidyverse)
library(grid)  # needed for inset maps in Task 4 — part of base R, no install required
ohio_counties <- sf::read_sf("./data/static_mapping/oh_counties.gpkg")
ohio_census <- sf::read_sf("./data/static_mapping/oh_counties_DP2020.csv")
glimpse(ohio_counties)
glimpse(ohio_census)
head(ohio_census$geoid)
head(ohio_counties$GEOID)
head(ohio_counties$GEOIDFQ)
county_join <- left_join(ohio_counties, ohio_census, join_by("GEOIDFQ"  == "geoid"))
glimpse(county_join)
# I chose to look at the proportion of females that are ages 20-24
county_census <- county_join %>% mutate(females20to24 = as.numeric(f20to24) / as.numeric(female))
glimpse(county_census)
hist(county_census$females20to24)
map_ohio <- tm_shape(county_census) + tm_polygons(fill = "females20to24", fill.scale = tm_scale_intervals(style = "jenks", values = "brewer.purples", n = 5), fill.legend = tm_legend(title = "Proportion of Females that are Ages 20-24")) + tm_borders(lwd = 2, col = "black") + tm_scalebar(position = c("right", "bottom"))
map_ohio
sa <- ohio_counties %>% dplyr::filter(NAME %in% c("Portage", "Summit")) %>% st_make_valid()
oh_region <- sf::st_bbox(sa) %>% sf::st_as_sfc()
oh_places <- sf::read_sf("./data/static_mapping/oh_places.gpkg") %>% st_make_valid
sf::st_crs(oh_places) == sf::st_crs(sa)
sa_places <- st_intersection(sa, oh_places)
glimpse(sa_places)
municipal_boundaries <- tm_shape(sa_places) + tm_borders() + tm_text("NAME.1", size = 2/5, remove.overlap=T)
municipal_boundaries
oh_parks <- sf::read_sf("./data/static_mapping/oh_parks.gpkg") %>% st_make_valid %>% st_transform(st_crs(sa))
sa_parks <- st_intersection(sa, oh_parks)
glimpse(sa_parks)
park_feats <- tm_shape(sa_parks) + tm_polygons(fill = "FEATTYPE", palette="Greens")
park_feats
port_water <- sf::read_sf("./data/static_mapping/tl_2022_39133_linearwater/") %>% st_make_valid
summ_water <- sf::read_sf("./data/static_mapping/tl_2023_39153_linearwater/") %>% st_make_valid
sa_water <- dplyr::bind_rows(port_water, summ_water) %>% st_transform(st_crs(sa))
sa_waters <- sa_water %>% mutate(intersects_park = lengths(sf::st_intersects(., sa_parks)) > 0)
glimpse(sa_waters$intersects_park)
streams <- tm_shape(sa_waters) + tm_lines(lwd = 1.5, col = "intersects_park", palette="Paired")
streams
map_local <- park_feats + streams + municipal_boundaries
map_local
dem <- terra::rast("./data/static_mapping/neoh_dem.tif") %>% terra::project(., sf::st_crs(sa)$wkt) %>% terra::crop(., sa)
#I chose to use the pipe hear to do this is one line of code, and I used the example of this from that inclass_static_mapping.Rmd, but I chose to project it first because I tried it and it did not take forever
dem_layer <- tm_shape(dem) +
tm_raster(col.scale = tm_scale_continuous(values = "terrain")) +
tm_shape(sa) + tm_polygons(fill_alpha = 0.3)
dem_layer
map_local_elev <- dem_layer + park_feats + streams +
tm_scalebar(position = c("center", "bottom")) +
tm_compass(type = "8star", position = c("right", "bottom"))
map_local_elev
dem_layer
map_local_elev <- dem_layer + park_feats + streams +
tm_scalebar(position = c("center", "bottom")) +
tm_compass(type = "8star", position = c("right", "bottom"))
map_local_elev
tmap_arrange(map_ohio, map_local_elev, ncol = 1, asp = NA)
# ---- Step 1: Build the inset (context) map ----
# This is the small map of all of Ohio that shows where our study area is.
# oh_region is the bounding box polygon you created in Task 2.1.
map_inset <- tm_shape(ohio_counties) + tm_polygons() +
tm_shape(oh_region) + tm_borders(lwd = 3, col = "red") +
tm_layout(bg.color = "lightblue") +
tm_scalebar(position = c("left", "bottom"))
# ---- Step 2: Normalize dimensions ----
# A viewport is defined in "normalized parent coordinates" (npc) from 0 to 1.
# If we just use width and height directly, maps with very different aspect ratios will appear distorted. norm_dim() calculates the width and height as fractions of the longest dimension, so that the aspect ratio is preserved.
norm_dim <- function(obj) {
bbox <- st_bbox(obj)
width  <- bbox[["xmax"]] - bbox[["xmin"]]
height <- bbox[["ymax"]] - bbox[["ymin"]]
w <- width  / max(width, height)
h <- height / max(width, height)
return(unit(c(w, h), "snpc"))
}
# Calculate dimensions for both maps
# The main map is based on the study area bounding box (oh_region)
# The inset map is based on all of Ohio
main_dim <- norm_dim(oh_region)  # fill in: use oh_region
ins_dim  <- norm_dim(ohio_counties)  # fill in: use the all-Ohio sf object
# ---- Step 3: Define viewport positions ----
# A viewport is a rectangular region of a graphics device.
# main_vp defines the size of the main map within the graphics window.
# ins_vp defines the size AND position of the inset.
main_vp <- viewport(width = main_dim[1], height = main_dim[2])
ins_vp <- viewport(
width  = ins_dim[1] * 0.4,          # scale the inset to 40% of normalized Ohio width
height = ins_dim[2] * 0.4,          # scale proportionally
x      = unit(1, "npc") - unit(0.5, "cm"),  # right edge of device minus a small margin
y      = unit(0.01, "npc"),          # near the bottom of the device
just   = c("right", "bottom")        # anchor point for the inset
)
# ---- Step 4: Draw ----
# grid.newpage() clears the device.
# print(..., vp = ...) draws a tmap within a specific viewport.
# pushViewport() makes the main viewport the "active" coordinate system, so the inset is positioned relative to the main map, not the whole device.
grid.newpage()
print(map_local_elev, vp = main_vp)
pushViewport(main_vp)
print(map_inset, vp = ins_vp)
#read in ohio data
ohio_counties <- sf::read_sf("./data/static_mapping/oh_counties.gpkg")
ohio_census <- sf::read_sf("./data/static_mapping/oh_counties_DP2020.csv")
#join the data
county_join <- left_join(ohio_counties, ohio_census, join_by("GEOIDFQ"  == "geoid"))
#make a column for the proportion of population count normalized by the are of land in the county
county_pop <- county_join %>% mutate(pop_per_area = as.numeric(poptotal) / (ALAND))
#subset data to the northeastern region of ohio
northeast_oh <- county_pop %>% dplyr::filter(NAME %in% c("Portage", "Summit", "Ashtabula", "Lake", "Geauga", "Trumbull", "Cuyahoga", "Mahoning", "Stark", "Lorain", "Medina", "Wayne", "Columbiana"))
#made choropleth map to show ohio county populations per land area in northeastern ohio
pop_map <- tm_shape(northeast_oh) + tm_polygons(fill = "pop_per_area", fill.scale = tm_scale_intervals(style = "jenks", values = "brewer.purples", n = 5), fill.legend = tm_legend(title = "Total Population per unit of Land Area")) + tm_borders(lwd = 2, col = "black") + tm_scalebar(position = c("right", "bottom"))
pop_map
#read in raster data to look at land cover, project it to be the same crs as the ohio data, and crop it to the northeastern ohio data
land_cover <- terra::rast("./data/ohio/neoh_nlcd.tif") %>% terra::project(., sf::st_crs(county_pop)$wkt) %>% terra::crop(., northeast_oh)
#map the raster land cover data for northeastern ohio
lc_map <- tm_shape(land_cover) +
tm_raster(col.scale = tm_scale_continuous(values = "terrain")) +
tm_shape(northeast_oh) + tm_borders(lwd = 2, col = "black")
lc_map
#arrange the maps side-by-side to view pattern
tmap_arrange(pop_map, lc_map, ncol = 1, asp = NA)
#arrange the maps side-by-side to view pattern
tmap_arrange(pop_map, lc_map, ncol = 1, asp = NA)
grid.newpage()
print(map_local_elev, vp = main_vp)
pushViewport(main_vp)
print(map_inset, vp = ins_vp)
# Step 1: Build the inset map
# All Ohio counties with a red bounding box showing the study area location
map_inset <- tm_shape(oh_joined) + tm_polygons() +
tm_shape(oh_region) + tm_borders(lwd = 2, col = "red") +
tm_layout(bg.color = "lightblue") +
tm_scalebar(position = c("left", "bottom"))
# Step 1: Build the inset map
# All Ohio counties with a red bounding box showing the study area location
map_inset <- tm_shape(ohio_counties) + tm_polygons() +
tm_shape(oh_region) + tm_borders(lwd = 2, col = "red") +
tm_layout(bg.color = "lightblue") +
tm_scalebar(position = c("left", "bottom"))
map_inset
# Step 2: Normalize dimensions
norm_dim <- function(obj) {
bbox <- st_bbox(obj)
width  <- bbox[["xmax"]] - bbox[["xmin"]]
height <- bbox[["ymax"]] - bbox[["ymin"]]
w <- width  / max(width, height)
h <- height / max(width, height)
return(unit(c(w, h), "snpc"))
}
main_dim <- norm_dim(oh_region)     # study area bounding box
ins_dim  <- norm_dim(oh_joined)     # all-Ohio extent
ins_dim  <- norm_dim(ohio_counties)     # all-Ohio extent
# Step 3: Define viewport positions
main_vp <- viewport(width = main_dim[1], height = main_dim[2])
ins_vp <- viewport(
width  = ins_dim[1] * 0.4,
height = ins_dim[2] * 0.4,
x      = unit(1, "npc") - unit(3.3, "cm"),
y      = unit(0.01, "npc"),
just   = c("right", "bottom")
)
# Step 4: Draw the map
grid.newpage()
print(map_local_elev, vp = main_vp)
pushViewport(main_vp)
print(map_inset, vp = ins_vp)
# Step 4: Draw the map
grid.newpage()
print(map_inset, vp = ins_vp)
# Step 4: Draw the map
grid.newpage()
print(map_local_elev, vp = main_vp)
print(map_inset, vp = ins_vp)
library(tmap)
data(World)
tm_shape(World) +
tm_polygons() +
tm_scalebar()
map_local_elev
map_local_elev <- dem_layer + park_feats + streams +
tm_scalebar(position = c("center", "bottom")) +
tm_compass(type = "8star", position = c("right", "bottom")) + tm_title("Map of Elevation, Parks, and Streams in Ohio's Summit and and Portange Counties")
map_local_elev
map_local_elev <- dem_layer + park_feats + streams +
tm_scalebar(position = c("center", "bottom")) +
tm_compass(type = "8star", position = c("right", "bottom")) + tm_title("Map of Elevation, Parks, and Streams in Ohio's Summit and and Portange Counties", size = 2)
map_local_elev
map_local_elev
map_local_elev <- dem_layer + park_feats + streams +
tm_scalebar(position = c("center", "bottom")) +
tm_compass(type = "8star", position = c("right", "bottom")) + tm_title("Map of Elevation, Parks, and Streams in Ohio's Summit and and Portange Counties", size = 5)
map_local_elev
map_local_elev <- dem_layer + park_feats + streams +
tm_scalebar(position = c("center", "bottom")) +
tm_compass(type = "8star", position = c("right", "bottom")) + tm_title("Map of Elevation, Parks, and Streams in Ohio's Summit and and Portange Counties", size = 0)
map_local_elev
map_local_elev <- dem_layer + park_feats + streams +
tm_scalebar(position = c("center", "bottom")) +
tm_compass(type = "8star", position = c("right", "bottom")) + tm_title("Map of Elevation, Parks, and Streams in Ohio's Summit and and Portange Counties", size = 0.5)
map_local_elev
map_local_elev
grid.newpage()
print(map_local_elev, vp = main_vp)
#pushViewport(main_vp)
print(map_inset, vp = ins_vp)
